<%- include header.ejs %>

<main>

    <%- include env_buttons.ejs %>

    <div class="i-image-with-text">
        <% if (environment !== 'v' && environment !== 'i') { %>
        <img src="/img/I<%= Math.floor((Math.random() * 3) + 1) %>.png" class="responsive-img hide-on-med-and-down i-image-with-text" style="width: 100%">
        <img src="/img/I<%= Math.floor((Math.random() * 3) + 1) %>_small.png" class="hide-on-large-only i-image-with-text responsive-img">
        <div class="i-welcome-content">
            <div class="row" align="center">
                <h3><strong>INSTRUCTORIO</strong></h3>
            </div>
            <div class="row" align="center">
                <p class="flow-text">
                    ¿Estás buscando clases a domicilio? <strong>Instructorio</strong> tiene justo lo que necesitas.
                    <br>
                    <br>
                    En <strong>Instructorio</strong> encontrarás instructores expertos en diferentes
                    áreas para que reserves tu clase con quien más te guste.
                </p>
            </div>
        </div>
        <% } else if (environment === 'i') { %>
        <img src="/img/IV<%= Math.floor((Math.random() * 3) + 1) %>.png" class="responsive-img hide-on-med-and-down i-image-with-text" style="width: 100%">
        <img src="/img/IV<%= Math.floor((Math.random() * 3) + 1) %>.png" class="hide-on-large-only i-image-with-text" style="width: 100%">
        <div class="i-welcome-content">
            <div class="row" align="center">
                <h3><strong>BIENVENIDO</strong></h3>
            </div>
            <div class="row" align="center">
                <p class="flow-text">
                    Encuentra aquí institutos asociados que ofrecen sus cursos para que aprendas sobre lo que más te gusta.
                </p>
            </div>
        </div>
        <% } else { %>
        <img src="/img/IV<%= Math.floor((Math.random() * 3) + 1) %>.png" class="responsive-img hide-on-med-and-down i-image-with-text" style="width: 100%">
        <img src="/img/IV<%= Math.floor((Math.random() * 3) + 1) %>.png" class="hide-on-large-only i-image-with-text">
        <div class="i-welcome-content">
            <div class="row" align="center">
                <h3><strong>BIENVENIDO</strong></h3>
            </div>
            <div class="row" align="center">
                <p class="flow-text">
                    Encuentra aquí diversos cursos virtuales* para que aprendas sobre lo que más te gusta sin moverte de donde estás.
                </p>
            </div>
        </div>
        <div class="container">
            <p class="black-text">
                * Los cursos presentados aquí corresponden a recomendaciones. Instructorio no es el propietario de dichos cursos,
                únicamente los promocionamos y damos a conocer. Para más información consulta nuestros
                <a href="/terminos" target="_blank">Términos y Condiciones.</a>
            </p>
        </div>

        <% } %>
    </div>
    <% if (environment !== 'v' && environment !== 'i') { %>
    <%- include teacher_cards.ejs %>
    <% } %>
    <% if (environment === 'v') { %>
    <br>
    <div class="row" align="center">
        <%- include virtual_courses_cards.ejs %>
    </div>
    <% } %>
    <% if (environment === 'i') { %>
        <br>
        <div class="row" align="center">
            <%- include school_cards.ejs %>
        </div>
    <% } %>
</main>

<%- include footer.ejs %>